/**
 * Messaging channel adapters – barrel export
 *
 * Each adapter implements the ChannelAdapter interface defined in
 * common/types/messaging.ts so Accomplish can treat every platform uniformly.
 */

export { WhatsAppAdapter } from './whatsapp-adapter.js';
